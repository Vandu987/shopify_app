<div class="nt-section">
  <h2 class="nt-heading text-center">{{ section.settings.heading }}</h2>

  <div class="nt-grid">
    {% for block in section.blocks %}
      {% assign product = all_products[block.settings.product] %}
      {% if product %}
        <div class="nt-product-card">
          <a href="{{ product.url }}">
            <div class="nt-image-wrapper">
              {% assign second_image = product.images[1] %}
              <img class="nt-main-image" src="{{ product.featured_image | img_url: '400x' }}" alt="{{ product.title }}">
              {% if second_image %}
                <img class="nt-hover-image" src="{{ second_image | img_url: '400x' }}" alt="{{ product.title }}">
              {% endif %}

              {% if product.compare_at_price_max > product.price %}
                {% assign discount = product.compare_at_price_max | minus: product.price | times: 100 | divided_by: product.compare_at_price_max %}
                <span class="nt-discount-badge">-{{ discount | round }}% SALE</span>
              {% endif %}
            </div>
          </a>

          <div class="nt-product-info">
           <div class="nt-stars">
              <div class="nt-stars">
              {% assign rating = product.metafields.custom.rating | default: 5.0 %}
              {% assign full_stars = rating | floor %}
              {% assign has_half_star = rating > full_stars %}
              
              {% for i in (1..5) %}
                {% if i <= full_stars %}
                  <!-- Full Star -->
                  <svg class="nt-star" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" width="20" height="20">
                    <path fill="#FFD700" d="M10 15l-5.878 3.09 1.123-6.545L.49 6.91l6.561-.954L10 0l2.949 5.956 6.561.954-4.755 4.635 1.123 6.545z"/>
                  </svg>
                {% elsif has_half_star and i == full_stars | plus: 1 %}
                  <!-- Half Star -->
                  <svg class="nt-star" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" width="20" height="20">
                    <defs>
                      <linearGradient id="halfGold{{ forloop.index }}" x1="0" x2="1" y1="0" y2="0">
                        <stop offset="50%" stop-color="#FFD700" />
                        <stop offset="50%" stop-color="#E0E0E0" />
                      </linearGradient>
                    </defs>
                    <path fill="url(#halfGold{{ forloop.index }})" d="M10 15l-5.878 3.09 1.123-6.545L.49 6.91l6.561-.954L10 0l2.949 5.956 6.561.954-4.755 4.635 1.123 6.545z"/>
                  </svg>
                {% else %}
                  <!-- Empty Star -->
                  <svg class="nt-star" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" width="20" height="20">
                    <path fill="#E0E0E0" d="M10 15l-5.878 3.09 1.123-6.545L.49 6.91l6.561-.954L10 0l2.949 5.956 6.561.954-4.755 4.635 1.123 6.545z"/>
                  </svg>
                {% endif %}
              {% endfor %}
            </div>


            <p class="nt-title">{{ product.title }}</p>
            <div class="nt-price">
              {% if product.compare_at_price_max > product.price %}
                <span class="nt-price-current">{{ product.price | money }}</span>
                <span class="nt-price-old">{{ product.compare_at_price_max | money }}</span>
              {% else %}
                <span class="nt-price-current">{{ product.price | money }}</span>
              {% endif %}
            </div>
          </div>
        </div>
      {% endif %}
    {% endfor %}
  </div>
</div>



{% schema %}
{
  "name": "Now Trending",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Now Trending"
    }
  ],
  "blocks": [
    {
      "type": "product",
      "name": "Product",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Select Product"
        }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [
    {
      "name": "Now Trending",
      "category": "Custom",
      "blocks": [
        {
          "type": "product"
        },
        {
          "type": "product"
        },
        {
          "type": "product"
        }
      ]
    }
  ]
}
{% endschema %}
